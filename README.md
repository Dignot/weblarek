# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run start
```

или

```
yarn
yarn start
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Типы данных (`src/types/index.ts`)

- `Product` — интерфейс товара.
- `BuyerData` — интерфейс данных покупателя.
- `PaymentMethod` — тип вида оплаты.
- `OrderDTO` — объект заказа для отправки на сервер.
- `OrderResponseDTO` — объект ответа сервера.
- `ProductListDTO` — объект списка товаров.
- `ValidationResult` — структура результата валидации данных покупателя.

---

## Классы слоя «Модели» (`src/components/`)

### Класс `Catalog`

Отвечает за хранение и работу с массивом товаров.

Методы:

- `saveProducts(items: Product[])` — сохранить массив товаров.
- `getProducts(): Product[]` — получить массив товаров.
- `getProductById(id: string)` — найти товар по ID.
- `saveSelected(productOrId)` — сохранить выбранный товар.
- `getSelected()` — получить выбранный товар.

---

### Класс `Cart`

Отвечает за хранение корзины.

Методы:

- `getItems()` — получить товары.
- `addItem(product: Product)` — добавить товар.
- `removeItem(productOrId)` — удалить товар.
- `clearItems()` — очистить корзину.
- `getTotal()` — сумма товаров.
- `getCount()` — количество товаров.
- `hasItem(id: string)` — проверка наличия товара.

---

### Класс `Buyer`

Отвечает за данные покупателя.

Методы:

- `saveData(partial: Partial<BuyerData>)` — сохранить данные.
- `getData()` — получить данные.
- `clearData()` — очистить данные.
- `validateData()` — валидация данных (каждое поле + общая валидность).

---

## Класс коммуникационного слоя

### Класс `WebLarekService`

Отвечает за работу с сервером, использует класс `Api` (композиция).

Методы:

- `fetchProducts(): Promise<Product[]>` — загрузить каталог (`GET /product/`).
- `createOrder(payload: OrderDTO): Promise<OrderResponseDTO>` — отправить заказ (`POST /order`).

---

## Основной скрипт (`src/main.ts`)

- Создаются экземпляры классов `Api`, `WebLarekService`, `Catalog`, `Cart`, `Buyer`.
- Выполняется загрузка каталога с сервера и сохранение в модель.
- В консоль выводятся данные, полученные методами моделей.
- Для теста вызываются методы всех классов (добавление товара в корзину, сохранение покупателя, валидация и оформление заказа).

## Классы слоя Представления (`src/components/view`)

### Класс BaseCard

Абстрактный класс представления карточки товара. Содержит базовую логику отображения данных продукта (название, цена, категория, изображение) в HTML-элементе.

- Поля класса

titleEl Ссылка на DOM-элемент с названием товара (.card**title)
priceEl Ссылка на DOM-элемент с ценой (.card**price)
categoryEl Ссылка на DOM-элемент с категорией (.card**category)
imageEl Ссылка на DOM-элемент с изображением (.card**image)
id string Уникальный идентификатор товара
product Product Объект текущего продукта, переданный в render()
events IEvents Шина событий для обмена сигналами между компонентами

- Методы

constructor(container: HTMLElement, events: IEvents) container – HTML-элемент карточки, events – шина событий void Находит внутри контейнера нужные подэлементы (title, price, category, image) и сохраняет их в поля
render(data: Product): HTMLElement data – объект товара HTMLElement Заполняет карточку данными товара: устанавливает текст, категории с модификаторами, изображение. Сохраняет продукт в поле product. Возвращает результат super.render(data)

### Класс CardBasket

- Поля класса
  deleteBtn Ссылка на кнопку удаления товара (.basket**item-delete)
  indexEl Ссылка на элемент с индексом товара (.basket**item-index)
  id (унаследовано) ID товара из Product
  events (унаследовано) событий для обмена между компонентами
  product (унаследовано) Объект товара, переданный в render()
- Методы
  Метод Параметры Возвращает Описание
  constructor(container: HTMLElement, events: IEvents) container – HTML шаблон карточки в корзине, events – система событий
  render(data: Product & { index?: number })

### Класс CardCatalog

- Поле класса
  titleEl Узел заголовка товара
  priceEl Узел для отображения цены
  categoryEl Узел категории товара
  imageEl Изображение товара
  id Идентификатор товара
  product Полный объект товара
  events Шина событий
  container Корневой DOM-элемент карточки
- Методы
  constructor(container: HTMLElement, events: IEvents) Инициализирует карточку, вызывает конструктор базового класса BaseCard, подписывается на клик по карточке, чтобы вызвать событие выбора товара.
  render(data: Product): HTMLElement Заполняет карточку данными товара (использует базовый render из BaseCard) и сохраняет товар в поле this.product.

### Класс BasketView

- Поле класса
  render(items: Product[], total: number): void items — массив товаров (Product[]), находящихся в корзине.
  total — итоговая сумма корзины.

- Методы
  constructor(events: IEvents) events — экземпляр шины событий (IEvents), который используется для отправки событий при удалении товара из корзины.

### Класс CardPreview

- Поле класса

button Кнопка "В корзину" / "Удалить из корзины"
inCart находится ли товар в корзине (true/false)
titleEl Элемент для названия товара
priceEl Элемент для цены
categoryEl Элемент категории
imageEl Элемент изображения
id Идентификатор товара
product Сохранённый объект товара
container Корневой DOM-элемент
events Событийная шина

- Методы
  constructor(container, events) — корневой DOM-элемент карточки
  events: IEvents — объект для работы с событиями void Вызывает конструктор BaseCard, находит кнопку и вешает обработчики кликов
  render(data) - объект товара HTMLElement Отрисовывает карточку, сохраняет товар и запрашивает текущее состояние "в корзине"
  setInCart(state) находится ли товар в корзине void Обновляет текст кнопки в зависимости от состояния ("В корзину" / "Удалить из корзины")

### Класс CatalogView

- Поле класса
  Имя Тип Описание
  template HTML-шаблон карточки товара, используемый для генерации списка
  container Контейнер, внутри которого будут отображаться карточки товаров
  events IEvents Системный объект для работы с событиями (эмит и подписка)
- Методы
  constructor(container, events) DOM-элемент каталога
  events: IEvents Сохраняет контейнер и события, ищет HTML-шаблон карточки, выбрасывает ошибку если не найден
  render(products) Очищает контейнер, клонирует шаблон для каждого товара, создаёт CardCatalog и вставляет в DOM

### Класс Header

- Поле класса
  basketButton Кнопка корзины в шапке
  counterEl Элемент отображения количества товаров в корзине
  events Эмиттер событий, используется для оповещения системы
- Методы
  constructor(container, events)— DOM-обёртка шапки
  events: IEvents — глобальный эмиттер void Находит элементы внутри шапки, привязывает обработчик на кнопку корзины (cart:open)
  setCounter(count) - count: number — количество товаров

### Класс ModalView

- Поле класса
  container Корневой контейнер модального окна
  events IEvents Эмиттер событий для взаимодействия с другими компонентами
  contentEl Внутренняя область для динамического содержимого модалки
  closeBtn Кнопка закрытия модального окна
- Методы

constructor(container: HTMLElement, events: IEvents) container — DOM-элемент модалки
events — глобальный эмиттер Инициализация модального окна и навешивание обработчиков закрытия
open(content: HTMLElement) content — узел, который надо отобразить в модалке Открывает модалку с переданным содержимым
close() — Закрывает модалку
openProduct(product: Product) product — данные товара Создаёт и открывает карточку предпросмотра товара
openCart(items: Product[], total: number) items — товары в корзине
total — сумма Открывает корзину
updateCart(items: Product[], total: number) items, total Обновляет содержимое корзины, если она уже открыта
private fillCart(container: HTMLElement, items: Product[], total: number) container — корневой узел корзины
items, total Заполняет корзину списком товаров и кнопками
openOrder() — Открывает форму заказа
openContacts() — Открывает форму с контактами
openSuccess(total: number) total — списанная сумма Открывает окно успешного оформления заказа

### Класс OrderPresenter

- Поля класса
  events IEvents Эмиттер событий, через который происходит взаимодействие между компонентами
  modal ModalView Управление отображением модальных окон на этапе оформления заказа
  cart Cart Корзина с товарами (используется для получения данных при завершении заказа)
  buyer Buyer Модель покупателя, в которую записываются данные из формы
- Методы
  constructor(events: IEvents, modal: ModalView, cart: Cart, buyer: Buyer) Внедрение зависимостей Создаёт presenter и вызывает init()
  private init() — Подписывается на события: открытие формы, завершение шагов, валидации
  private handleOrderStep1(orderNode: HTMLElement) orderNode — DOM-элемент формы доставки Обрабатывает первый шаг заказа: выбор оплаты и адреса
  private handleOrderStep2(contactsNode: HTMLElement) contactsNode — DOM-элемент формы конт
